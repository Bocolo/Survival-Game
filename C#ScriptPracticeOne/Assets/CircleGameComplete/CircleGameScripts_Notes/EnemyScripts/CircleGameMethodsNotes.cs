using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CircleGameMethodsNotes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
     - TakeDamage();
     - Count Enemy Destroy - BeInvinsible() - seconds - reset enemy count 
     -  Size Reduction - destroy?
     - Power Ups
     */
}
